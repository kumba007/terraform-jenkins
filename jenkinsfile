pipeline {
  agent any
  environment {
    PATH = "${PATH}:${getTerraformPath()}"
  }
  stages{
    stage('Terraform init'){
      steps{
        sh "terraform init"
      }
    }
  }
}

def getTerraformPath(){
  return tool name: 'aws-terraform', type: 'terraform'
}
